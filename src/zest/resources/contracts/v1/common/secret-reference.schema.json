{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:zest:contracts:v1:secret-reference",
  "title": "SecretReference",
  "description": "Reference to a secret resolved at the Worker execution boundary after Core authorization. MUST NOT contain token, password, api_key, or any secret value.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "secret_ref",
    "purpose"
  ],
  "properties": {
    "secret_ref": {
      "type": "string",
      "minLength": 1,
      "description": "Opaque SecretPort reference. Not the secret value."
    },
    "purpose": {
      "type": "string",
      "minLength": 1,
      "description": "Why this reference is attached to the job."
    },
    "version_hint": {
      "type": "string",
      "minLength": 1,
      "description": "Optional rotation/version hint. Not a credential."
    }
  }
}
