# Optional. Use only when RESEARCH_OSD_LOG_PATH is set.
# journald remains the process log unless the application writes this file.

/var/log/research-os/*.log {
    weekly
    rotate 8
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
}
