# Optional. Use only when ZEST_LOG_PATH is set.
# journald remains the process log unless the application writes this file.

/var/log/zest/*.log {
    weekly
    rotate 8
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
}
